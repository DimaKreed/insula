# Copy to .env.local and fill in what the current phase needs.
# Phase 0 (TTS bake-off) needs only ONE of the TTS provider blocks below.

# --- Database (Neon) --- Phase 1
DATABASE_URL=

# --- Auth.js --- Phase 1
# Generate with: npx auth secret
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Magic-link email + transactional email
RESEND_API_KEY=
# Verified sender for magic-link email, e.g. "Insula <login@yourdomain.com>"
AUTH_EMAIL_FROM=

# --- Translation --- Phase 1
# Which provider translates EN->RO: gemini | claude   (default: gemini)
TRANSLATION_PROVIDER=gemini
# Gemini free tier — https://aistudio.google.com/apikey (no card needed)
GEMINI_API_KEY=
# Optional: override the default free-tier Flash model (gemini-3.5-flash).
# List what your key can use: GET https://generativelanguage.googleapis.com/v1beta/models
GEMINI_MODEL=
# Only needed when TRANSLATION_PROVIDER=claude
ANTHROPIC_API_KEY=

# --- TTS --- Phase 0 (bake-off) / Phase 2 (pipeline)
# Decided in the bake-off: azure (Free F0 tier, ro-RO-AlinaNeural).
# Which provider the app synthesizes with: elevenlabs | google | azure | openai
TTS_PROVIDER=
# ElevenLabs — https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
# Google Cloud TTS — service-account JSON, base64-encoded:
#   PowerShell: [Convert]::ToBase64String([IO.File]::ReadAllBytes("sa.json"))
GOOGLE_TTS_CREDENTIALS=
# Azure AI Speech — key + region slug (e.g. westeurope)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
# Optional: pin the voice. Default = the adapter's first voice for the language
# (Azure: ro-RO-AlinaNeural).
TTS_VOICE=
# Optional: pin the English voice used for Listen/Recall hint audio.
TTS_VOICE_EN=
# OpenAI TTS
OPENAI_API_KEY=

# --- Object storage --- Phase 2
# Where audio files are written: local | r2   (default: local)
# local writes public/audio/ and needs no account. r2 is required before the
# first deploy — Vercel's filesystem is read-only and ephemeral.
STORAGE_PROVIDER=local
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_URL=

# --- Background jobs --- deferred
# Phase 2 runs background work with after() from next/server — nothing to
# configure. These are only read once a queue is introduced.
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# --- Rate limiting (Upstash Redis) --- Phase 7
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
